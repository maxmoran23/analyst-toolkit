# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/

# Environment / secrets
.env
.env.*
*.local

# Logs / scratch
*.log
scratch/

# JVM / Gradle (quant-jvm)
quant-jvm/build/
quant-jvm/.gradle/

# frameworks/ synthetic data — regenerated from seed, never committed.
# The evidence DERIVED from it (evidence/) IS committed; the raw data is not.
frameworks/*/data/
# Same rule at the repo root: a generator that writes ./data/ instead of its own
# framework data/ dir must not smuggle regenerable fixtures into the repo.
/data/

# Codex silo — working material, reviewed and promoted deliberately, never published.
# Standard: ~/Desktop/Agents/_skills-library/codex-silo/SILO-STANDARD.md
codex/
